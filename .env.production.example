# Production Environment Configuration Template
# Copy this to .env.production and update with your production values
# NEVER commit this file to version control

# Application
APP_NAME="Renal Tales"
APP_VERSION="2025.v2.0"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://yourdomain.com
APP_TIMEZONE="Europe/Bratislava"

# Production Database (Remote WebSupport - All scenarios use remote database)
DB_CONNECTION=mysql
DB_HOST=mariadb114.r6.websupport.sk
DB_PORT=3306
DB_DATABASE=SvwfeoXW
DB_USERNAME=by80b9pH
DB_PASSWORD='WsVZOl#;D07ju~0@_dF@'
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci
DB_PREFIX=
DB_STRICT=true
DB_TIMEOUT=30
DB_SSL_VERIFY=false

# Test Database (Remote WebSupport - Same as main database)
TEST_DB_HOST=mariadb114.r6.websupport.sk
TEST_DB_PORT=3306
TEST_DB_DATABASE=SvwfeoXW
TEST_DB_USERNAME=by80b9pH
TEST_DB_PASSWORD='WsVZOl#;D07ju~0@_dF@'
TEST_DB_CHARSET=utf8mb4
TEST_DB_COLLATION=utf8mb4_unicode_ci
TEST_DB_PREFIX=test_

# Database Logging
DB_LOGGING_ENABLED=false
DB_LOG_FILE=storage/logs/database.log
DB_LOG_LEVEL=warning

# Doctrine Cache
DOCTRINE_METADATA_CACHE=redis
DOCTRINE_QUERY_CACHE=redis
DOCTRINE_RESULT_CACHE=redis

# Production Mail
MAIL_MAILER=smtp
MAIL_HOST=your-smtp-host
MAIL_PORT=587
MAIL_USERNAME=your-smtp-username
MAIL_PASSWORD=your-smtp-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@yourdomain.com
MAIL_FROM_NAME="Renal Tales"

# Logging (Production)
LOG_CHANNEL=file
LOG_LEVEL=warning

# Session
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Cache (Consider Redis for production)
CACHE_DRIVER=redis

# Redis (Recommended for production)
REDIS_HOST=your-redis-host
REDIS_PASSWORD=your-redis-password
REDIS_PORT=6379
REDIS_DB=0
REDIS_CACHE_DB=1

# Security (CHANGE THESE VALUES!)
APP_SECRET=generate-a-strong-unique-secret-key-here
JWT_SECRET=generate-a-strong-unique-jwt-secret-here
BCRYPT_ROUNDS=12

# File Storage (Production)
STORAGE_DRIVER=local
STORAGE_PATH=storage/files

# Additional Paths
LOG_PATH=storage/logs/app.log
CACHE_PATH=storage/cache
SESSION_PATH=storage/sessions

# All scenarios now use remote database
# Local development removed - all operations use remote WebSupport database
